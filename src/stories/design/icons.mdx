import { Meta, IconGallery, IconItem } from "@storybook/blocks";
import * as Icons from "@heroicons/react/24/solid";

<Meta title="Design System/Iconography" />

# Iconography

<IconGallery className="bg-white-smoke dark:bg-gunmetal">
  {Object.entries(Icons)
    .filter(([name]) => name !== "iconList")
    .map(([name, Icon]) => (
      <IconItem name={name}>
        <Icon className="text-gunmetal" />
      </IconItem>
    ))}
</IconGallery>
